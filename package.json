{
  "name": "ai-tutor",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "start": "vercel start"
  },
  "dependencies": {
    "@vercel/node": "^34.3.0", // Critical version update
    "@upstash/redis": "^1.22.0", // Session storage
    "zod": "^3.22.4" // Input validation
  },
  "engines": {
    "node": ">=18.x"
  }
}
